package org.gvi.algorithms.ri;

/** Which of the spec's two operational RI definitions (Section 7.1) produced a {@link RiResult}. */
public enum RiMethod {
    PHI_TEST_BREAKPOINT_SCAN,
    ISOLATE_CLASSIFICATION
}
